{
  "name": "quita",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "api": "json-server-auth db.json -r routes.json --port 3000",
    "app": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "json-server": "0.17.4",
    "json-server-auth": "2.1.0"
  }
}
